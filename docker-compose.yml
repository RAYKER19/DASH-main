services:
	backend:
		build:
			context: .
			dockerfile: backend/Dockerfile
		env_file:
			- backend/.env
		ports:
			- "8000:8000"

	frontend:
		build:
			context: ./frontend
			dockerfile: Dockerfile
		depends_on:
			- backend
		ports:
			- "5173:80"
